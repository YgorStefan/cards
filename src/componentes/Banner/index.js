import './Banner.css'

function Banner() {
    // return JSX (Como o react trabalha com a parte visual, entendendo o que é e faz um append no DOM)
    return (
        <header className="banner">
            <img src="/imagens/ccm.png" alt="Index principal"/>
        </header>
    )
}

export default Banner